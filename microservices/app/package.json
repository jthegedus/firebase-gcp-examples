{
  "name": "app",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "i": "yarn",
    "setup": "yarn firebase use --add",
    "deploy": "yarn firebase deploy --only hosting"
  },
  "dependencies": {},
  "devDependencies": {
    "firebase-tools": "^6.7.0"
  }
}
